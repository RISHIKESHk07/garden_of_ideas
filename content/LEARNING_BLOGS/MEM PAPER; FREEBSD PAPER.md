
# MY FINDINGS:

Long paper: ~113 pages, I guess we need to figure out how to read it completely.

---

## Introduction:

Mainly, we talk about how certain components in computers have been optimized to different degrees, resulting in a few of them having extraordinary breakthroughs and the rest becoming bottlenecks that we have today. Storage and RAM are some examples, while networking is an example of something that improved really fast.

The paper focuses on hardware improvements for storage and RAM, along with limited software optimizations as well, mainly caches which support the main memory (L1, L2, L3), etc. Now here are the three main ideas in the hardware optimization we explore:
- RAM hardware design (speed and parallelism)
- Memory controller designs
- CPU caches
- Direct memory access (DMA) for devices
So, the document mainly focuses on CPU cache, controller design, and DMA as well.  
Written in light of Linux, so yes, not something directly for macOS or Windows, I guess.  
Another paper to look out for is **"What Every Computer Scientist Should Know About Floating-Point Arithmetic"**

---

## Hardware Today

We have horizontally scaled rather than vertically scaled systems over time; quad cores have become the norm. Coming to the structure of the system, we have CPUs, Northbridge, and Southbridge. The Northbridge is responsible for the memory controller, which helps in working with RAM, and it is connected to every CPU through a bus (front-side bus). The Southbridge is responsible for working with I/O devices using buses like SATA, PCI-Express, USB, etc. All communication with devices passes through the Northbridge and Southbridge and then to the CPUs; all communication with RAM passes through the Northbridge. Communication between CPUs also goes through the Northbridge.

Few bottlenecks: any device that wants to communicate with RAM for data or any task has to go through the CPU, putting a burden on the CPU. In order to solve this, we introduce DMA (Direct Memory Access), which allows communication to occur without the need for CPU involvement, though this causes contention between DMA and CPU. The other bottleneck is the bus from the Northbridge to RAM; it is a shared bus with limited parallel access. But these days we have improved buses (DDR2, etc.) which increase bandwidth, and the Northbridge can interleave memory access across channels.

Now we involve multiple cores, hyper-threads, and processors trying to access RAM, so we need a way to schedule access in order to decrease latency; this is true for DMA as well. We also factor in the concept of access patterns when designing systems. We have other ways to increase bandwidth also; instead of attaching the memory controller to the Northbridge, we use a separate controller (not only in the Northbridge anymore), which helps in attaching the RAM. So now we can attach multiple of them, allowing for better access time in concurrent situations (access patterns) and across cores, by accessing multiple memory banks at once. The only issue is with the internal Northbridge bandwidth here.

Now, a more popular method (SMP systems, AMD’s Opteron processor) is to attach the memory controller and memory directly to the CPU instead of the Northbridge. Now the memory bandwidth is effectively increased (e.g., for quad cores), and this is called NUMA systems. The obvious drawback is that, while we want the system to behave as if memory access is uniform, the access time is non-uniform, since there is a difference between accessing local memory and accessing memory of another CPU, which requires going across connecting buses (multiple hops depending on cores and interconnects, e.g., IBM x445).

---

### RAM TYPES

Mention of SRAM and DRAM: the main reason we do not use SRAM for everything is cost and higher power consumption. SRAM is made up of 6 transistors (two cross-coupled inverters) and requires constant power for maintaining the stored state. We have variants with speed trade-offs, etc., not very important here. Also, in order to read and write, we use the word line and bit line; we read when the word line allows access, and the bit line difference contains the cell state. When we want to write to the cell, we charge up the bit line and open the word line, and due to reverse flow from the bit line we write into the cell.

DRAMs are more complicated, use less space than SRAM, and consume less energy, but require constant refresh as charge dissipates quickly. Coming to the details, because of the need for periodic refresh (which affects reading and writing capability), we have a refresh window of about 64 ms. Also, as charge builds up and dissipates, it requires time, which delays operations (it is not instantaneous like SRAM), and when we read the cell state we need to restore it again for subsequent operations.

Some more details: when we want to access DRAM, our virtual memory address is converted to a physical address, goes to the memory controller first, and after that it would naively require many lines (e.g., 32 bits → 32 lines), which does not scale well. So we encode and multiplex to reduce the number of lines; it is very difficult to maintain ~30 parallel signals compared to ~15. Another problem is that there is a delay when we update or access the cell state after using RAS and CAS, so the system must wait for these delays to ensure correctness. Also, needing ~30 address lines just to access RAM modules (RAM → modules → chips) is not feasible, especially if we want parallel access. To solve this, DRAM multiplexes the address internally using RAS and CAS, along with control lines to indicate operations.

---

DRAM Technical details: we are mainly looking at SDRAM (Synchronous DRAM) and DDR. In SDRAM, we have a clock frequency provided by the memory controller, and it determines the speed of the FSB (interface between Northbridge, CPUs, and RAM). Today’s buses are double or quad pumped, so we can push 2× or 4× the amount of data.

Read access protocol: we use RAS, CAS, address bus, data bus, and CLK. We read on rising edges of CLK, and we assert/deassert RAS & CAS signals when we receive the required row or column address. We have tRCD, which is the time after which we can send the column address (after sending the row address). After obtaining both, we have another wait time (CAS latency) before data is returned. We can also keep the row open (RAS active) and send multiple CAS signals to get sequential memory addresses efficiently. DDR works similarly.

Another part of the process is precharging, where we prepare the memory for subsequent operations. We use the WE (write enable) line to initiate a precharge cycle (tRP). After completing this interval, we can select the next row address. There is also a constraint (tRAS) on how long a row must remain active before precharging again. Refresh scheduling is important, as we need to refresh every ~64 ms to prevent data loss; the memory controller manages this using counters.

---

Memory types:

SDR SDRAM: frequency is the same for the memory cell array and the bus. To increase throughput, we would need to increase frequency, which is not feasible.

DDR1: uses the same frequency across components but transfers data on both clock edges, effectively doubling throughput using an I/O buffer.

DDR2: increases bandwidth further by using a faster I/O interface; since the cell array cannot operate at higher frequency, the I/O buffer helps achieve higher effective data rates.

DDR3: the I/O operates at around 4f, and with DDR signaling, achieves effectively higher bandwidth (8× of the cell array rate). It is also more energy efficient compared to DDR2 (based on the context of the paper).

Per channel of DDR1, DDR2, DDR3, we have various numbers of pins (e.g., ~240 for DDR2). DDR2 typically works with up to 2 modules per channel, while DDR3 often supports only 1 module per channel at higher frequencies.

As we are limited by the number of channels from the Northbridge, we could use memory controllers integrated per processor, but this introduces NUMA complications, which are not always desirable. Intel addressed this using another kind of DRAM called FB-DRAM (Fully Buffered DRAM). It uses a serial bus to the memory controller.

Advantages are:

1. More modules per channel can be use
2. More channels per memory controller can be used (narrower interface compared to parallel DDR2 bus)
3. The serial bus is designed to be full-duplex
4. It is feasible to implement differential signaling (two lines in each direction), increasing speed

This results in higher overall throughput (see performance table in the paper), though with added latency.

Some more important ideas: for DDR-based DRAM, we use parallel channels (often dual-channel) from the memory controller to DIMMs (RAM slots where we physically insert RAM sticks). In FB-DRAM, we can support up to ~6 serialized channels, and this increased number helps overcome the serialization/deserialization latency. With higher frequency, this can be a better option, as achieving similar performance on a wide parallel bus at high frequency is energy inefficient and difficult to maintain.

---

# CACHE

Here we first talk about how a CPU cores became faster than RAM output frequency , we see that both of these over time have comparable differnce in speed completely , we could match speed of the CPU cores at the cost of expensive hardware which is highly inefficent , and would like a brick of a laptop heavier than you , So we had to come with solutions which combined the use of SRAMs and DRAMs together , first naive implementation as to let the OS manage the SRAM address space (mapping virtual to SRAM address space , this is a hurdle in its own) , but this fails as even if we have a very fast Memeory access capablility the problem lies with synchornization acoss processors who will use this memory , this latency or complication likks the speed advantage entirely , so we do not invest into this method , second approach was more efficient where we use the SRAM as store of duplicates which can be accessed by any processors and use the DRAM as our main operating memory , this works better mainly because of the idea of spatial and temporal locality of code and data , spatial locality being that when we access a particular part of a memory their is a good chance that we will use the memory address around this point down the line of execution (arrays is good example or loops) ,temporal locality being we will invoke a certain data access / code section again over a certain strecth of time , Now this allows for speed ups using caches at 90 percent , and also remember we have limited size of cache meaning we cannot fill up entire programs (1GB program etc) directly into the cache right , but we are also not working on every instruvtion in program at once but only on a simple subset of instrutions , so we can use this to our advantage . This allows for easily for multiple process to use the cache together without worrying of over occupying the cache and starving other processes .

Now the systems we use are bit more complicated due to introduction of cache , we genrally maintain a cache for data and cache for code , as both of them generally work on a different subset of address spaces (Followed by Intel even today) , We mention the addition of higher caches like L2,L3 as we cannot economically grow L1 as its made of SRAM , so moving on we have multiple processors who in turn of multiple cores and who in turn use multiple threads , this how modern systems actually function , remember that cores have similar set of hard ware compoents meaning they can function independently while threads exactly have more shared architecture with a little independent registers which they can work on . Now for actually working of cache we use Tags to help with that , each tag helps us to identify if any requested cache line is present in the cache based on tag which derived using the memory address as we look in the cache based on a memory adsress , based on spatial locality it is better to always bring lines of data instead of single data words as seen in Hardware section , so in-order for this to happen our cache is made of 64 byte lines , and this reduces additional CAS , RAS at the DRAM level as well . Now when we want to write we have load the cache line into the cache , modify and mark it as dirty , which is then written to the main memory . When we make spcae in the cache we need to evict right , here we have two ideas exclusive and inclusive , exclusive pushes the L1data line to L2 and then accordingly to L3 if required (L2 is filled) , this is great as now we work with L1d only meaning its fast , in inclusive we contents of L1d present in L2 as well and so when we want to evict we need to do it from two places . We have too many types cache implementation even for a general x86 architecture across various inc , but the main point is that this form of memory abstraction powerfully enough to be applied to any system , coming SMP systems (multi processors) we need every processor to have a unified - single view of the memory (obviously you cannot have different views right ... ) , so you cannot just have separate caches , this is called cache coherency , coming to how we are make this work , in case of reads their will be no change when looking up across processor its fine but in case of writes when a processor try to do this , other processors having the same cache line will need to modify their local cached version through a protocol , a simple sophisticated approach is when a processor 2 is accessing a dirty flagged cache line in its cache and its still not written to main memory , here processor 2 get the content directly from the processor 1 without involving the main memory and further implementations memory controllers does the direct transfer and fills the main memory . MEIS protocol is a one such cache coherent protocol ,The outcome of all this can be summarized in a few simple rules: • A dirty cache line is not present in any other processor’s cache. • Clean copies of the same cache line can reside in arbitrarily many caches. If these rules can be maintained, processors can use their caches efficiently even in multi-processor systems. All the processors need to do is to monitor each others’ write accesses and compare the addresses with those in their local caches . Table mentioned about raw number of cycle for looking up L2 , L1d , main memory , these values feel a bit high but we can generally hide this cost partial due the fact of pipelining in modern CPUs where due pipelining certain data load operations are done in parallel before the required instruction needs it , this hides the cost but if this does not occur then we will take the full brunt . We have a interesting graph plotted between cycles vs working set size  of randomised RAM access operations , we can se that some operations take less than 10 cycles (we can assume that L1d could fit this access or program 's working set) , we also see the same for L2 is 28 cycles , and anything more suddenly balloons very fast as its to be accessed from the main memory .The motive was give us a glimpse about performance increase by writing cache efficient code is at a noticeable level not just in a few percentages .

Coming to different kind of caches implementations we have associativity as other first factor for the variants , commonly we have full associativity where every cache line in the cache has a unique tag id and we compare all the cache lines for search for a required target , obviously this in hardware level would require a large amount of circuitry which is not feasible , then Direct-mapped cache , we use a Tag , set , offset for every set bit combination we can map to a single cache line (we use multiplexer here for this ) and then a single comparator to check if the tag id is what we are looking for , highly scalable as when we have have higher memory the bit representation increases by logarithm terms , so circuit hardware increase is nominal at best , only drawback is that because the way or cache works or to be specify , how our programs based spatial and temporal locality we might always work upon / evict certain cache lines way more than others or the other cache lines are empty (if multiple processes request a certain cache line periodically and lets say we evict this cache line just before this to make room for something else ....) , Set associativity fixes this by allowing the set to have more than one cache line in it , this divides the cache lines to sets first and then we perform comparison in each set using comparators , scaling this easy as well , if more rows number of sets increase , in case of increase of associativity we have logarithmic increase here like direct map method . Coming to the effects we see a 40 percent less thrashing occur in set associative based on numbers page (20) , do remember we are working on a randomized working set , contagious memory is not common case all the time , we see sharp increase when moving from 512KB to 2MB to 4MB (L2 cache ) , and almost same after that , here after a certain increase the perf increase is almost neligible .Also a important point out  here ,when we talk shared caches in modern systems hyperthreaded , multi core , caches are shared , so effectively per core we might have lesser space to cache and the moment its not sufficient we default to larger L3 caches .

Inorder to go over some practical numbers we will be using a simple demonstration here we will measure the random access time in cycles when we have various working sets (the size for the program we are working on ...) and we have a parameter PAD which indicates the payload we add to the array of structs (linked list) elements , we uses various PAD values for coming to a conclusion about our caches along with all other stuff above , Now when we run Cycles vs working sets , we see that sammler sets which fits in L1d works fine , with speed of 4 cycles , when approaching L2 cache size limit we still have a predictable speed with 9 cycles , but a better than expected 14 cycles , this is because our processor is smart enough to prefetch data as its **sequential in nature** , and even after this L2 limit size , for increasing working set we have a plateau value around 14 cycle precisely due to prefetching  , originally we saw that after certain large working set size we need to talk to the memory directly (200+ cycles) , so quite the performance here . Now , Cycles vs Working sets (different PAD , 0 , 7 ,15) , here we see that till the L2 size limit PAD 0 requires less cycles because it size is small can be easily worked with , now coming to something larger like PAD 7 , we see that L2 is up to 28+ cycles this represents L2 caches proper speed approxmately , the see is simple PAD 7 means between two elements we have 7x8=56 bytes in gap , this does not fit directly in our cache lines either we need multiple cache lines to do this and the prefetcher cannot not work here as we can only transport a single element not more , and also prefetcher cannot load a new cache line every cycle as well , now coming the higher working sets we see this occur again PAD 15 and more , here the problem is that the prefetcher is limited to size of the OS page has crossing page boundries you get fetch page contents not related to the program and cause page fault , and find this was not intitated by the program (prefetching is processor feature) , now the processor does not know whether this request page is present or does not exist .

Cycles vs Working size sets (For TLB factor for two cases sequential layout vs special case random layout) , here we realise that when we use PAD 7 or above we need a new cache line to every element we iterate or work with , meaning now when we see this occur we still need a single page to hold all these 64 elements at once , but in the second special case let assume that every element required a single page (so , 1 page -> 1 element ... remaining part of page empty) , that would mean after 64 elements (64 pages which we hit so fast) , TLB cannot hold more than 64 entires ( number of pages TLB can hold is 64 ) , at working set size of 2 pow (13) is becames evident a TLB cannot hold more than this , so now the we need to search through our multi level page table (long search and expensive) resulting in our graph exploding here . We have a final demonstrations Cycles vs working sets (Addnext ,INc ,Follow) , follow is the condition we saw just now we this our benchmark , INc for every element we add a payload constant PAD[0] to it , so we writing here , so now the cache line is dirty and needs to be written back to main memory , Addnext is adds the next PAD[index] to current element , so this also has dirty page to write back , becuase of this write back operation we decrease the FSB bus 's bandwidth as we need to rewrite for every cache line and then after this we need to evict for new data which is being loaded for being worked upon (so we almost lost half the time here ... ) .Interestingly we see that Addnext in the smaller working sets (around L2 size limit here ) is performing comparable to INc because of the prefetch of next element in Addnext as we need to add its payload , this does stay the same for long either . We perform a simple demonstration of cycles observered when we have different cache sizes , P1: 32KB L1 , 1MB L2 , P2; 16KB L1 , 512KB L2 , 2MB L3 , P3; 32KB L1 , 2MB L2 , the main idea here obviously bigger cache better , but we observe that we can have region of good access time just below the working set of size of last layer cache , and the moment move out of this Lat cache size limit we have dramatic increase in access time .

So , till now we have seen how prefetching strategy by Processor saved L2 access times a lot , but in random workload this does not work anymore at all , we have graph (Page 23) , as the working set grows their is dramatic increase now , add if you see after a certain working sets we have access time greater than main memory acess here beacuse in the previous section remember we found that prefetching backfires when in random workload , it can by mistake call random unrelated pages or bytes thinking its useful , so now we have essentially made our situation worse then main memory lookup too 450 cycles . Now coming to a Table which shows that L2 cache misses doubles as our working set doubles , so the average access time per list element increases as our working set increases , with reason being TLB miss rate , when we randomise our entire single block of elements , its like having to note down 1000 table addresses for looking up 1000 elements but have a memory of 64 , we do not find required page we do page table lookup , but if we decrease the randomised to a smaller block (60) , then we could say search for all the required elements in 60 tables only , this means it fits in memory making the TLB misses less , we see from the Graph that our one block randomised case upper bounds all the other cases , and that TLB cache plays a important role here , if managed we could have a decent performance increase 34% for now .

Coming to Writes , we have primarily two types of write policies , write through and write back , write through means we have to write back to main memory and cache in immediately , write back does it a bit differently mainly we flag the cache line as dirty and then when we evict this cache line we write it back to main memory , write back is more efficient as with the bandwidth we have already its preferred to space the main memory write , so most processors use this , but it has a issue when we are working with multi processor architecture where when one processors marks it dirty and the second processor requests a read we need to maintain the idea that this was dirty and needs to be written to main memory before a new read . Another two policies we observer ate write combining and uncacheble  , both of these not backed main memory (we do not exactly write into RAM) , write combining is something more seen Graphics devices where we combine multiple writes and data transfer them to the device , mainly because the data transfer is quite costly to do , uncacheable is memory addresses which cannot be cached , like memory mapped address ranges , so these are reserved to talk to devices like LED etc directly , and you do not want to cache . Coming to Multi processor support , first how do we send data between processors , direct access is a no go as this is not fast enough , and other option is transfer of data when required , when do we do this this , when one of the processor has a dirty page and another processor needs to read or write it , how do we detect this , no broadcast as we use very frequent operations and we can't send a broadcast everytime this occurs , so MEIS protocol to the rescue , a simple state machine diagram is given to for this , it maps the state of each cache line in every processors and accordingly moves forward , so coming to each state , and remeber state change detection trivial and we use the address bus as well , **(Modified: The local processor has modified the cache line. This also implies it is the only copy in any cache. Exclusive: The cache line is not modified but known to not be loaded into any other processor’s cache. Shared: The cache line is not modified and might exist in another processor’s cache. Invalid: The cache line is invalid, i.e., unused)**, initially all cache lines are empty and so we use INVALID , when we fill them up and we have writes for all of them then Modified , if read would be Shared or Exclusive , Now Modified state , if we local read we do have to change state , if we local write we do not have change state , if we have remote read we need to move Shared state and send the content of cache line as well  , if we have remote write  we mark this as invalid  and send the content of cache line to the requesting processor , this is called Requesting ownership operation which is a bit expensive . Now Shared state , local read and remote read we do no have any change in state as we are just using the existing copies , in case of local write we need change state to modified and also sed RFO messages to other processors who have this cache line and make their state invalid as it is stale copy , remote write would mean we make this invalid , Exclusive state is similar to shared state , only difference is local write operation , where we just change the state to modified but do not need to broadcast any RFO as their no other processor with this cache line . So , we the imprtance of RFO messages here we will broadly require to use RFOs when we first need to migrate a thread acoss the processors or when two processors are accessing the same cache lines . Now to some limitations remember that MESI needs all the processor to indicate that they have recieved messages or else how could be sure to process a transisition as completed , on NUMA systems latency is present and bus collisons to occur as well contributing to the lacentency , and other prominent issue with FSB bus which is a shared resource among the processors which can slowed down the entire communication as well .

Coming to multi threaded processors and their aspect of cache is that , when we limit ourself to just to no modifying memory (now writes and only reads) , we see a 18% performance decrease in 2 threaded execution and 34 % in 4 threaded . Here , the sole attribute for this is the bottlneck in bandwidth of bus from the processors to Memory controller and bandwidth of the bus from memory controller to memory modlues , and also important point here is this is a sequentail reading case , so all the threads are prefetching as well , so when we cross the working set the size of L3 , even the 2 threaded execution is severly bottlnecked . Now , coming to modify memory we see that , 18% percent in 2 threaded and a dreaded 94% percent in 4 threaded , Sequential nature so we try prefetching and write backs for consisitent memory in main memory saturates all of our bandwidth , and observing the graphs we have we see that as more threads are invovled the L1d cache is providing lesser effectiveness , as its gets chopped into pieces and gets overwhelled very fast , and moving forward to the case of randomaccess addnextLast , here we explode 1500 cycles when we want to work with a reasonably long linked list ,... , coming to efficient throughput we know that we have some good numbers (Table page 28) ,here obviouly 2 threaded has a possible scale up limit of 2 and 4 threaded is 4 , we have graph which shows that the scale is occurs good portion of the graph when less than the L3 cache the moment we cross this , 2 threaded and 4 threaded crash to similar levels of scale up around 1.5 , this is a big reason we do not go above 4 threaded systems in general computers . Coming to the last part we talk about hyperthreaded case where we have a single physical core emulate two logical cores , we have 2 hyper threads per core , remeber that a hyperthreaded cores shares all the physical properties and resources , except for the register set , the CPU is responsible for time multiplexing of these cores inorder work efficiently . Inorder to get the most of this idea if we multiplex our work then our combined runtime from the two hyperthreaded cores as to be bteer than single core 's work , Now we see how can we best use this , assume our single threaded execution has 60 % chache hit and 40% miss rate , here our CPU waits idle for 40% , so we can introduce even a slightly inferior second thread to help us be more efficient , and now assume the 95% cache hit rate , here CPU is to busy meaning , their is almost no way second threaded coulb be spun up as inorder for the overall runtime to be efficent , we both threades to be effieciently , possiblly we need the second thread atleast 80% cache hit , but if we involve a second core we cut our cache size per core to half and that means our already perfectly fast single threaded L1d cache we start getting thrashed decreasing effiecency . A big reason why we don't think to use hyperthreads simply .

Coming to modern processors , we use virtual memory , virtual address meaning different processes use the same virtual address but pointing to different physical addresses , and conversion to physical is done through MMU , this non trvial and has to be done down the pipeline after a while , meaning it takes time , so if we index or tag our cache with physical memory based addresses then for faster L1 cache will be slowed down as we need to convert it first and this makes L1 lose its speed performance we use it for , so we use virtual adresses as tags for L1 caches , as for L2,L3 we use physical memory as by the time we need to use L2 or cache and retrieve the information , we should be able to convert the virtual address to physical addresses . Also has L1 is small per process we can flush information more easily and tolerate it (because every process has different set of virtual meory) , partial flush in case of page table change of the process . Incase of L2,L3 they are too large to consider this at all in the first place . Coming to a conslusion this is not in hands of the programmer to manipulate so we cannot argue much about this , this include page replacement policy (LRU) as well .

Coming instruction cache , here remeber that code 's flow patterns are more easily recogniseable then data , compilers generate good optimisied code for this very reason , and finally code generally has good spatial and temporal locality , So we know that CPU core execution surpassed L1 cache lookups , Instruction processing is done in pipelines , decoding , processing parameters , execution , these pipelines take a long time to complete and we use our hero *pipelining* , parallel processing of instructions (you know the basic idea :| right ), and we also have CPU stalls as well which slow down stuff , coming to the actual dicussion now , CSIC systems generally cache the decoded instructions  (trace cache is term for this) is L1i instead of encoded instructions , as when cache hits occur they can directly pull decoded instructions and ignore starting steps of the pipeline giving a good boost , for L2 caches we have unified for both data and instructions ,best performance is done when the processor can prefetch instructions with good decisions and generated code is small , but when we use pipelining we can tend to create longer and complex code generation cycles . in light of all these Branch predictors in CPUs play a massive role .

SMC not worth looking up ...... :< , not looking at this , old concept with honestly nothing great except the possible speed boosts in very specific situations .

We talk about Cache bandwidth and memory bandwidth , this paragraph will be showing performance difference among various different versions , focus on the conclusion points , we measure the performance by using SSE instruction loading 16 bytes at once , First machine: P4 (Net burst intel processor) , for read workload it goes upto 16 bytes until L1 size limits and drops significantly and then their is another drop in curve when our working set cannot fit all our pages in TLB , finally we sitting at 5.3 bytes for larger working sets , here as data is sequential we prefetch , but remember prefetched data not propagated to L1 , write performance is around 4bytes/cycle for the smallest working sets (less than L1d) , meaning L1d uses write through mode and further on it keeps decreasing , same for copying as well , it keeps decreasing till 0.5 bytes/cycle , Second Machine: P4 processor but with two hyperthreads , the graph looks halfed compared to the first machine 's values , mainly becuase hyperthreads share the cache and bandwidth of bus , so its gets divided among the the threads , we have half the cache we can work with resulting in these numbers , Third Machine: Core 2 ( dual core processor ) , here we have a L2 cache shared by both crores and is upto 4 times larger than P4 processor , here we are also prefetching data but unlike P4 we are placing them in L1d cache , resulting in 16 bytes/cycle for a goo amount of time , and also as L2 is large enough , we can maintain a high read performance for a good while and then a slight drop due to TLB misses , Write policy is not write through ,(its write back) so the even for smaller working sets we have a good high performance (P4 strugles because of write through) read which slowly falls down as we increase the working set , Machine 4: Core 2 with 2 hyperthreads per core , hre the read performance is similar to what we had in Machine 3 , but coming to write and copy , we see that we have performance equal to fetching data from the main memory (worst performance) even for working sets smaller than L1d , mainly due to RFOs messages across the processors , and now we see that L1d in every processor cannot handle this properly and have to flush it to the L2 cache , and now we see the performance rise about 50% , as data is stored in the cache we don't need anymore RFOs per these cache lines and its bounded by L2 latency speed , the speed at this size is half of the single threaded , as we have essentially divided our bandwidth by 2 . We discuss about AMD as well but both of them do have great performance to really look up (refer to page 34) .

Cache 's Critical word load , the main crux here is that DRAM operates at sending 64 bites , so we require 8 cycles for 64 bytes (a cache line 's size) , so lets say that a cache miss occurs and we need to pull the word at the end of the cache line inorder for our program to execute further , so we think straightforwardly we see that for the first word we might take 4 cycles but inorder to fetch the last word we need a additional of 30 cycles at least , this results a slow down , in order to get around this we can send the required word with help of the memory controller first and then slowly send the remaining words , here we want to obviously prefetch as well , here we see one conflict occuring that we might have a in flight prefetch for a critical word and also ask ask just for the critical word from the meory controller at once , this results in some wasted time , as we need to wait for the operation and cannot change the order midway , we see a graph for randvs sequential access for various working sets , it works pretty much as expected sequential operations are flucating alot , and randomized starts to falter after L2 size limit and then reachs a stable point .

We come to Cache placement , here we see that how programmers show note their cache architectures for better performance , we mention about various generation of multi cores we built and how caches behaved in them , Early multi-core processors had no shared caches at all. ,Later Intel models have shared L2 caches for dualcore processors ,For quad-core processors we have to deal with separate L2 caches for each pair of two cores. There are no higher level caches ,AMD’s family 10h processors have separate L2 caches and a unified L3 cache . Having no shared cache is not exactly the best solution , but when we think about it , we see that most programs a single threaded and do not have overlapping memory requirements , but we still various os libs we need to be shared for optimisation right , coming to Intel 's dual core processor we see that L1 is not shared per processor and all others are shared L2,L3 , this is a good balanced structure , as when we have more common software/ overllaping memory  between any two threads then can pullwd from the larger L2,L3 and incase of no such need we need effiently manage the L2,L3 cache , so that no one core monopolies the cache space , We have a simple test demonstration where we have P1 : Fixed SSE writing/reading of 2MB , P2: Variable reading/writing , so we have four combinations out of these , we see that for writing 2MB operation pair starts falling before 1MB , this is pure cache eviction here  , as their is no RFO ocuuring at all , we need good cache management algos to make sure we have some good performance

```
The future of cache design for multi-core processors will lie in more layers. AMD’s 10h processor family makes the start. Whether we will continue to see lower level caches be shared by a subset of the cores of a processor remains to be seen (in the 2008 generation of processors L2 caches are not shared). The extra levels of cache are necessary since the high-speed and frequently used caches cannot be shared among many cores. The performance would be impacted. It would also require very large caches with high associativity. Both numbers, the cache size and the associativity, must scale with the number of cores sharing the cache. Using a large L3 cache and reasonably-sized L2 caches is a reasonable trade-off. The L3 cache is slower but it is ideally not as frequently used as the L2 cache.

``` 

Finally we see a graph around FSB influence 

