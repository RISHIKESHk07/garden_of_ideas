- [link](https://www.makingsoftware.com/chapters/how-is-data-stored)

# MY FINDINGS
- The mention of registers , caches (SRAM) , Main memory (DRAM) , SSDs
- The latency of three as well know , and do remember both data and instructions are stored in L1 cache and we have L2 and L3 cache as well present we want maintain speed and have a good size for our caches meaning L1 is kept smaller and we use a multi level cache help out with size reqs
- SRAMs are made by transistors , a memory cell is constructed by using two inverter logic gates to create a stable feedback loop , its then seperated from other cells by the word line and using word line transistors we control bit lines . Inorder to know what state is present in the cell when the CPU reqs it we open access and then a voltage is send to one of bit lines , and based on increase of the voltage at the other end we can say if its a 1 or 0 . Inorder to write we do something similar where we ope access and then we set the bit lines with required voltage to change the cell's state .
- So in SRAM we have a row of cells with word lines and bit lines , the problem here is that we have a constant requirement for storing meaning if we make a huge SRAM we have huge power consumption due to the long word line and bit line , so thats why we break a large requirement into smaller Caches with less power requirement , the reason why L1 cache is the smallest always and kept that way .
- So if we used SRAM instead of DRAM everywhere we would be economical value of computer would fall into oblivion compared to what we have today ... they would 10x bulkier too.
- DRAMs also store data but unlike SRAM they do not require constant power to function but require interval power to refresh their data so that its not lost (electronics lose stored energy so we have revitalise it regularly) , similar in structure to SRAM but uses a capacitor
- Coming to the read and write operations in DRAMs its similar to SRAMs as well just that we have (bank number , col , row ) addresses when we need to access the DRAM , here a DRAM has many banks in them , during read and write we precharge the DRAM and then use the read driver or write driver at the end of the device connected to the DRAM's column multiplexer . We need to refresh the entire DRAM every 64ms or we risk losing data , so it takes a few milli seconds for this to happen and is could be disaster as we cannot read or write during this time , so the memory controller smartly finds ways to mitigate this by ideas like shorter refresh cycle , refresh the required rows to be read and then carry out the remaining row refresh process etc . Arranging memory which are similar read at times in the same place is vital , as it saves cycles of time and tasks for the CPU processor . Remember that DRAM still require constant power to keep working .
- SSDs when you want to store ata across power cycles (do need constant power to operate..) , here we have use single tap cell to do this (triple tap cell these days ) , they work in principle similar to a transistor where we have a gate which induces a electric field to attract electrons in a current channel and displace them across a dielectric material with specific dimensions (built just for this 8nm) , and using the current observed and know voltage we find out the charge built up here , the displacement here mentioned is quantum tunnelling which ruptures the material overtime making the ssd not a great option for long term storage. Now we have a similar idea in structure here as well we have vertical column structure of gates with horizontal control gates and at the top we have bit line which drains our data / charge . A vertical column is called a string , a array of strings is called a page , this is called NAND flash memory , when we arrange it in a 3D structure its  called 3D NAND flash.
- Finally to mention the SSD had a controller to interface with the flash memory and it also uses a DRAM buffer with a DRAM controller to help make subsequent requests faster .We try to store data across blocks (similar DRAM banks ) as it helps in parallel read as when we read or write from a block its locked and cannot be accessed by someone else .
- Good old HDDs , not much about data bing stored here , cylinders where not talk about more , so here we have read/write head which actually read the magnetic field to find out whats present here , it uses the difference meaning stronger field means a change bit value here etc , high tolerance and precision based device as scale of mechanical op on the platter is in nano meters . Sectors , tracks , cylinders , two r/w heads on each disc . Defragmenting algos required to make its performance good , and it also uses DRAM buffer . The Longest lifetime of storage , so best used for backups . 