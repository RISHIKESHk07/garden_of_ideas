- [link_infoq](https://www.infoq.com/presentations/arcticdb-db-no-server/)
- Starts out by mentioning the kind of stuff they deal with at a trading firm in term of data and esoteric sources they use , mentions that MAN group uses python 's Dataframe as a standard data format for communication between their system

```
For the data warehouse stuff on the furthest left, this makes sense, you need to archive off data somehow and work with it in an in-memory structure that makes sense. As you go further along, we also use DataFrames to exchange data between systems in this pipeline. Importantly, you also care a lot about versioning data, because data vendors will often correct data after they've delivered it to you, and it will have this cascading effect on all the calculations that you've done off of that data. You really need to be able to track modification to data over time and see not just what's the most up-to-date, most corrected version of the data, but also what's the data at the time that my system saw it, so you can reproduce the behavior of your systems. We clearly need some good technology for dealing with pandas DataFrames, serializing them, reading them out really fast, versioning them. -- InfoQ transcript of the video
```

- The system prior to this used a Mongo cluster and did serialisation of data in the DB to pandas 's dataframes , the issue with this system was the huge mongo cluster .
- One of the motivations for building the object storage system was their S3 read and write bandwidth they mentioned .
- We talk about building a thick client lib which connects to a object storage . The the conference talk outlines that i will discuss how to acheive atomicity , durability and consistency , how to manage global state without synchronization , CRDTs , versioning .
- Mentions a simple tree structure based solution with index and version node on top these , over here new version appended form a another tree which points to older version tree to attach them to each other  , and remember the data in the tree is immutable over here  except for the mutable key we attach this structure too , which is only visible to the client 
- A bit about io pipelining work here , figuring out a scheduling algorithm to parrallelize the io work and its obviously async as we do not want any sync based task blocking our threads for a good time ,idea of compression which could be used at the metadata layer at the top rather than bottom or maybe a adaptive compression algorithm for working different kinds of data types .
- Mentions that one place where we have a bog cost of compute occurs is in conversion of formats to export to user , presents a CPU bound graph telling that string copy and format conversion is the most repeated/heavy task .
- Issue with client lib architecture is the version compatibility issues you have deal with .
- Global state management here , we talk how its hard to maintain global state without coordination of write operations , the simple approach maybe reading through the entire data to look through versions of the data to make sure something is completely deleted , inefficient , we could use a symbol cache (a metainfo about the operations we are applying ie db.write(appl) -- > symbol cache.add(appl t1) t1 is time , we apply this for all operations and compact to find wheather a object exists ) , but clock drift is a problem you cannot around here , if the compaction operation here is out of order then your db might be wrong .
- A formal idea about Conflict free replicated data types (CRDT) , the main idea is that you can maintain a global state without coordination here , three or more replicas of state when transformed with changes arriving from other nodes eventually converges to the same stae , they do not depend on the order of changes arriving , editing in collobrating software is the best example out their.  
- CRDTs do not have many know DS we could use , Grow-Set , Two-Phase Set , Observer-removed Set are some examples .
- We talk about the third idea here called Observer-removed Set , where we add a metadata tag on every operation and we have a particular unique hash for this metadata , now we propagate these around , on remove operation we publish state as well . Check the example here , its well explained .
- The blog explains that instead of using timestamps in our metadata we use versions in the CRDT instead 
- Garbage collections , Compaction and accumulation of tiny objects is a issue with CRDTs. So you need to be very sure about their use case .
- Conditional writes in AWS ,Microsoft etc .
- The bigger thing is that the above allows for distributed locks to implemented but its a approximated lock , no mutual exclusion exactly .
- Fencing in lock mechanism.